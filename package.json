{
  "name": "cosmocopia",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "art",
    "web"
  ],
  "scripts": {
    "web": "npm --workspace web run dev",
    "render": "npm --workspace art run render",
    "test:art": "npm --workspace art run test",
    "gen:bindings": "stellar contract bindings typescript --network testnet --contract-id $NEXT_PUBLIC_PLANET_CONTRACT --output-dir web/lib/planet-bindings --overwrite"
  },
  "dependencies": {
    "playwright": "^1.58.0"
  }
}
